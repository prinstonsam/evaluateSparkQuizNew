package com.griddynamics.quiz.domain

final case class Flights(
                        airline: String,
                        airport: String,
                        airlineDelay: Int
                        )